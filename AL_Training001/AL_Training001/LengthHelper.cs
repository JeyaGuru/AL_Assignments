using System;

namespace AL_Training001
{
    //Write a class LengthHelper with function findLength that takes string as parameter and returns the length of string.
    //This can be a class libarary/console.
    public class LengthHelper
    {       
        public int FindLength(string input)
        {
            return input.Length;
        }
    }
}
